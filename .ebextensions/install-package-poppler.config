packages:
  yum:
    poppler-utils: []  # Replace with the actual package name

commands:
  install_package_poppler:
    command: "sudo yum install -y poppler-utils"  # Explicit install command if needed
